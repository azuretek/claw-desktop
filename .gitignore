node_modules/
dist/
*.log
.DS_Store

# Generated at pack time by scripts/build-info.js. A commit cannot contain its
# own hash, so a checked-in copy would be wrong by construction; its absence is
# what makes a source run report itself as one.
src/build-info.json
